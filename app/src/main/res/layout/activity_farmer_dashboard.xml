<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F3F6F4">

    <!-- GREEN HEADER -->
    <LinearLayout
        android:id="@+id/topGreenHeader"
        android:layout_width="match_parent"
        android:layout_height="160dp"
        android:background="@drawable/bg_green_top"
        android:orientation="vertical"
        android:padding="24dp">

        <TextView
            android:text="Farmeta"
            android:textColor="@android:color/white"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <TextView
            android:text="Smart Farming Dashboard"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:layout_marginTop="6dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
    </LinearLayout>

    <!-- WHITE SECTION -->
    <LinearLayout
        android:id="@+id/whiteSection"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/topGreenHeader"
        android:layout_marginTop="-40dp"
        android:background="@drawable/bg_white_top_round"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- SEARCH -->
        <EditText
            android:id="@+id/etSearch"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:hint="Search crops..."
            android:background="@drawable/bg_search"
            android:paddingStart="16dp"
            android:layout_marginBottom="12dp"/>

        <!-- CATEGORY -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="none">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/btnAll"
                    style="@style/CategoryActive"
                    android:text="All"/>

                <TextView
                    android:id="@+id/btnRice"
                    style="@style/CategoryNormal"
                    android:text="Rice"/>

                <TextView
                    android:id="@+id/btnTomato"
                    style="@style/CategoryNormal"
                    android:text="Tomato"/>

                <TextView
                    android:id="@+id/btnPotato"
                    style="@style/CategoryNormal"
                    android:text="Potato"/>

            </LinearLayout>
        </HorizontalScrollView>

        <!-- LIST -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvCrops"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:paddingTop="12dp"/>

    </LinearLayout>

</RelativeLayout>
